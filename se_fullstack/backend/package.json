{
  "name": "aircraft-classification-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  
    "install": "npm install && npm install --prefix ../frontend && npm run build --prefix ../frontend",
    "start":   "node server.js"
  },
  "dependencies": {
    "cors":      "^2.8.5",
    "dotenv":    "^16.0.3",
    "express":   "^4.18.2",
    "multer":    "^1.4.4",
    "pg":        "^8.10.0"
  }
}
